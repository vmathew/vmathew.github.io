_site
# macOS metadata. A bare pattern with no slash matches at every level.
.DS_Store
*.DS_Store
# Local only: record of seeded counter values. Never commit — the repo is public.
counter-worker/SEED-LEDGER.local.md

# Scratch drafts and generated files — not part of the site
Claude outputs/
